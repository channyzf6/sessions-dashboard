{
  "name": "sessions-dashboard-mcp",
  "version": "0.3.0",
  "description": "A live dashboard for every Claude Code session you're running. See activity in real time, organize sessions into groups, and share a browser across sessions.",
  "type": "module",
  "main": "index.mjs",
  "bin": {
    "sessions-dashboard-mcp": "./index.mjs",
    "sessions-dashboard-daemon": "./daemon.mjs"
  },
  "scripts": {
    "start": "node index.mjs",
    "daemon": "node daemon.mjs",
    "postinstall": "playwright install chromium"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "playwright": "^1.48.0"
  },
  "engines": {
    "node": ">=18"
  }
}
